https://github.com/ssguschenka/weblarek.git

# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Данные

#### Интерфейс IProduct
Содержит данные о товаре

Поля интерфейса:
`id: string` - ID товара
`title: string`- Название товара
`image: string` - Картинка товара
`category: string` - Категория товара
`price: number | null` - Цена (может отсутствовать)
`description: string` -Описание товара

#### Интерфейс IBuyer
Содержит данные о покупателе

Поля интерфейса:
`payment: TPayment` - Способ оплаты товара. Можно выбрать оплату онлайн, при получении, либо выбор оплаты отсутсвует  
`address: string` - Адресс покупателя, который он указывает
`email: string` - Электронная почта покупателя
`phone: string` - Номер телефона покупателя

#### Интерфейс IOrder
Содержит данные, которые отправляем на сервер

Поля интерфейса:
`buyer: IBuyer` - Данные о покупателе
`items: IProduct` - Список покупаемых товаров

#### Интерфейс IResponseOrder
Содержит данные ответа сервра

Поля интерфейса:
`id: string` - айди заказа
`total: number` - сумма заказа

#### TPayment
Тип для спосоов оплаты
`type TPayment = 'card' | 'cash' | ''`


#### Модели данных
Классы, которые отвечают за хранение и работу с данными.

#### Класс Catalog

Содержит логику работы с данными товаров, которые можно купить в приложении

Конструктор:
`constructor()` - Ничего не перевем

Поля класса:
`products: IProduct[]` - поле для хранения массива товаров, которые будут отображаться в каталоге
`item: IProduct` - полe для хранения выбранного товара

Методы класса:
`saveProducts(products: IProduct[]): void` - Метод для сохранения массива товаров, полученного в параметрах метода
`getProducts(): IProduct[]` - Метод для получения массива товаров из каталога
`getItemById(id: string): IProduct | undefined`- Метод для получения товара по его id
`saveItem(item: IProduct): void` - сохранение товара, полученного в параметрах метода, для подробного отображения
`getItem(): IProduct | null` - Метод для получение товара для подробного отображения.

#### Класс Basket

Содержит логику работы с данными товаров, выбранных покупателем для покупки.

Конструктор:
`constructor()` - Ничего не передаем

Поля класса:
`products: IProduct[]` - поле хранит массив товаров, выбранных покупателем для покупки.

Методы класса:
`getProducts(): IProduct[]` - Метод для получения массива товаров, которые находятся в корзине
`addItem(item: IProduct): void` - Метод для добавления товара, полученного в параметре, в массив корзины
`deleteItem(item: IProduct): void` - Метод удаления товара, полученного в параметре из массива корзины
`clearBascet(): void` - Метод очистки корзины.
`getPriceProducts(): number` - Метод получения стоимости всех товаров в корзине. Возвращаем число
`getCount(): number` - Метод получения количества товаров в корзине. Возвращаем число
`hasItem(id: string): boolean` - Проверка наличия товара в корзине по его id, полученного в параметр метода.

#### Класс Buyer

Содержит логику работы с данными покупателя, которые тот должен указать при оформлении заказа.

Конструктор:
`constructor()` - Ничего не передаем

Поля класса:
`payment: TPayment` - поле хранит вид оплаты
`address: string` - поле хранит данные введенного адреса
`phone: string` - хранит телефон покупателя
`email: string` - хранит почту покупателя

Методы класса:
`saveBuyer(data: Partial<IBuyer>): void` - Метод сохранения данных покупателя. Параметр - данные покупателя, которые нужно сохранить
`getBuyer(): IBuyer` - Метод получения всех данных покупателя
`clearBuyer(): void` - Метод очистки данных покупателя
`validateBuyer(data:  Partial<IBuyer>):  Partial<Record<keyof IBuyer, string>>` - Метод для проверки валидности данных. Возвращает обьект с ошибками

#### Слой коммуникации

#### Класс LarekApi
Использует композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и будет получает с сервера объект с массивом товаров.

Конструктор:
`constructor(obg: IApi)` - Принимает объект, соответствующий интерфейсу IApi

Поля класса:
`obg: IApi` - обьект для запросов

Методы класса:
`getProductList(): Promise<IProduct[]>` - Метод делает get запрос на эндпоинт /product/ и возвращает объект, полученный от сервера, в котором находится массив товаров
`postOrder(order: IOrder): Promise<IResponseOrder>` - Метод делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода, а возвращает объект, подтверждающий покупку на определенную сумму



#### Слой Прелставления

#### Интерфейс IEvents
Содержит информацию об обрабтчике событий

Поля интерфейса:
`emit<T extends object>(eventName: string, data?: T): void` - Инициировать событие с данными

#### Интерфейс IHeader
Содержит информацию о счетчике

Поля интерфейса:
`counter: number` - счетсик товаров в корзине

#### Класс HeaderView extends Component<IHeader>
Cодержит логику работы с элементами в шапке сейта

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - Принимает элемент шапки сайта и обработчик событий

Поля класса:
`basketButton: HTMLButtonElement` - поле для хранения элемента кнопки
`counterElement: HTMLElement` - поле для хранения элемента счетчика

Методы класса:
`set counter(value: number)` - метод для установки нового значения счечика


#### Интерфейс IGallery
Содержит информацию о каталоге товаров

Поля интерфейса:
`catalog: HTMLElement[]` - массив элементов товаров в каталоге


#### Класс GаlleryView
Cодержит логику работы с элементами в каталоге товаров

Конструктор:
`constructor(container: HTMLElement)` - Принимает элемент каталога товаров

Поля класса:
`catalogElement: HTMLElement` - поле для хранения элемнета карточки товара

Методы класса:
`set сatalog(items :IGallery)` - метод для добавления элементов товаров в каталог



#### Интерфейс ICard
Содержит информацию о карточке товара

Поля интерфейса:
`title: string` - название товара
`price: number | null` - цена товара


#### Класс CardView<T> extends Component<ICard & T>
Родительский класс для карточек товаров

Конструктор:
`constructor(container: HTMLElement)` - Принимает контейнер карточки товара

Поля класса:
`priceElement: HTMLElement` - поле для хранения элемента цены товара
`titleElement: HTMLElement` - поле для хранения элемента названия товара

Методы класса:
`set price(value: number | null)` - метод для установки цены в карточку товара
`set title(value: string)` - метод для установки названия в карточку товара



### Тип TCardCatalog
Тип для отрисоки нужных элементов в карточке
`type TCardCatalog = Pick<IProduct, "image" | "category">;`


### Интерфейс ICardActions
Содержит информацию о функции, которая выполнится при клике пользователем по элементу кнопки в карточке

Поля интерфейса:
`onClick: (event: MouseEvent) => void;`

#### Класс CardCatalogView extends CardView<TCardCatalog>
Cодержит логику работы с элементами в карточках каталога товаров

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` - Принимает контейнер карточки в каталоге и функцию которая будет исполняться при клике на карточку

Поля класса:
`categoryElement: HTMLElement` - поле для хранения элемента категории товара
`imageElement: HTMLImageElement` - поле для хранения элемента картинки товара

Методы класса:
`set category(value: CategoryKey)` - Метод установки категории товара в карточке
`set image(value: string)` - Метод установки картинки товара в карточке.



### Тип TCardCatalog
Тип для отрисоки нужных элементов в преввью карточки
`Pick<IProduct, "image" | "category" | "description">`


#### Класс class CardPreviewView extends CardView<TCardCatalog>
Содержит логику работы с элементами в карточках модального окна

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` - Принимает контейнер карточки и функцию которая будет исполняться при клике на кнопку карточки

Поля класса:
`textElement: HTMLElement` - поле для хранения элемента описания товара
`categoryElement: HTMLElement` -  поле для хранения элемента категории товара
`imageElement: HTMLImageElement` -  поле для хранения элемента картинки товара
`buttonElement: HTMLButtonElement` -  поле для хранения элемента кнопки на карточке товара

Методы класса:
`set text(value: string)` - метод для установки описания в карточку товара
`set category(value: CategoryKey)` - метод для установки категории товара в карточке
`set image(value: string)` - метод для установки картинки товара в карточке.
`set button(value: string)` -метод для установки текста на кнопке в карточке
`set disabled(value: boolean)` -метод для дезактивации кнопки в карточке товара


### Интерфейс ICardBasket
Содержит информацию о карточке товара 

Поля интерфейса:
`index: number` - индекс товара


#### Класс CardBasketView extends CardView<ICardBasket>
Содержит логику работы с элементами в карточках в корзине

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` - Принимает контейнер карточки в корзине товаров и функцию которая будет исполняться при клике на кнопку в карточке 

Поля класса:
`indexElement: HTMLElement` - поле для хранения элемента индекса товара
`buttonRemoveElement: HTMLButtonElement` - поле для хранения элемента кнопки удаления товара

Методы класса:
`set index(value: number)` - метод для установки индеса товара в корзине


### Интерфейс IModal
Содержит информацию о модальном окне

Поля интерфейса:
`container: HTMLElement` -контейнер модального окна
`content: HTMLElement` - содержимое
`button: HTMLButtonElement` - кнопка закрытия модального окна

#### class ModalView extends Component<IModal>
Содержит логику работы с элементами модального окна

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` - Принимает контейнер модальнонго окна и обработчик событий

Поля класса:
`containerElement: HTMLElement` - поле для хранения элемента контейнера модального окна
`contentElement: HTMLElement` - поле для хранения элемента контента модального окна
`buttonCloseElement: HTMLButtonElement` - поле для хранения элемента кнопки закрытия модального окна

Методы класса:
`open(content: HTMLElement): void` - метод открытия модального окна. Устанавливается контент в модальное окно и присваивается класс 'modal_active' контейнеру
`close(): void` - метод ззакрытия модального окна. Удаляется контент в модальном окно и удаляется класс 'modal_active' у контейнера


### Интерфейс ISuccess
Содержит информацию об окне оформленного заказа

Поля интерфейса:
`text: string` - информация об списанных средствах
`button: HTMLButtonElement` - кнопка выхода из окна успешной покупки


#### Класс SuccessView extends Component<ISuccess>
Содержит логику работы с элементами карточки оформленного заказа

Конструктор:
`constructor(container: HTMLElementб events: IEvents)` -Принимает контейнер окна завершенного заказа и обработчик событий

Поля класса:
`textElement: HTMLElement` - поле для хранения элемента описания карточки покупки
`buttonElement: HTMLButtonElement` - поле для хранения элемента кнопки карточки покупки

Методы класса:
`set text(value: string)` - метод для установки суммы покупки


### Интерфейс IBasket
Содержит информацию о корзине товаров

Поля интерфейса:
`list: HTMLElement[]` - список товаров в корзине

#### Класс BasketView extends Component<IBasket>
Содержит логику работы с элементами в корзине: список товаров, сумма всех товаров, кнопка "Оформить"

Конструктор:
`constructor(container: HTMLElement, evenst: IEvents)` - Принимает контейнер корзины товаров и обработчик событий
 
Поля класса:
`listElement: HTMLElement` - поле для хранения элемента карточки товара
`buttonElement: HTMLButtonElement` - поле для хранения элемента кнопки оформления заказа
`priceElement: HTMLElement` - поле для хранения элемента суммы всех товарав в корзине


Методы класса:
`set items(items: HTMLElement[])` - Метод установки элементов корзины
`set price(value: number)` - Метод установки суммы продуктов в корзине
`set submitDisabled(value: boolean)` - Метод для отключения кнопки оформления заказа


### Интерфейс IForm 
Содержит информацию о форме оформления заказа

Поля интерфейса:
`button: HTMLButtonElement` - кнопка в форме 
`errors: {}` - ошибки


#### Класс FormView<T> extends Component<IForm & T>
Содержит логику работы с общими элементами форм: кнопки отправки, элементы ошибок

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents, private submitEventName: string)` - Принимает контейнер формы оформления заказа, обработчик событий и названия событий

Поля класса:
`buttonSubmit: HTMLButtonElement` - поле для хранения элемента кнопки отпраки
`errorsElement: HTMLElement` - поле для хранения элемента ошибкок

Методы класса:
`set errors(errors: Record<string, string>)` - метод для отрисовки сообщения об ошибке
`set disabled(value: boolean)` - метод для дезaктивации кнопки отправки данных


### TOrder
Тип для отрисовки нужных данных покупателя
`type TOrder = Pick<IBuyer, "payment" | "address">`


#### Класс OrderView extends FormView<TOrder> 
Содержит логику работы с элементами формы офоррмленния заказа: кнопки способа оплаты, поле ввода адреса доставки

Конструктор:
`constructor(form: HTMLFormElement, events: IEvents)` - Принимает контейнер формы оформления заказа и обработчик событий
   
Поля класса:
`buttonsPayElement: HTMLButtonElement[]` - поле для хранения массива кнопок вида способа оплаты заказа 
`imputAddress: HTMLInputElement` - поле для хранения поля ввода адреса доставки

Методы класса:
`set pay(value: string)` - метод установки активной кнопки
`set address(value: string)` - метод установки адреса покупателя


### TContacts 
Тип для отрисовки нужных данных покупателя
`type TContacts = Pick<IBuyer, "phone" | "email">`

#### Класс ContactsView extends FormView<TContacts>
Содержит логику работы с элементами формы офоррмленния заказа: поля ввода электронной почты и телефона покупателя

Конструктор:
`constructor(form: HTMLFormElement, events: IEvents)` - Принимает контейнер формы оформления заказа и обработчик событий
   
Поля класса:
`inputРhone: HTMLInputElement` - поле для хранения поля ввода телефона покупателя
`inputЕmail: HTMLInputElement` - поле для хранения поля ввода почты покупателя

Методы класса:
`set phone(value: string)` - Метод установки телефона покупателя
`set email(value: string)` - Метод установки почты покупателя



### Презентер
События:
`catalog:change` - изменения в каталоге товаров
`card:select` - выбор карточки товара
`product:changed` - изменения выбранного товара для просмотра
`product:basket` - добавление товара в корзину / удаление из корзины в превью карточки
`modal:closed` - закрытие модального окна
`basket:open` - открытие корзины товаров
`basket:changed` - изменение корзины товаров
`product:delete` - удаление товара из корзины 
`products:buy` - открытие окна оформления заказа
`button:payment` - выбор способа оплаты
`form:address` - заполение адреса доставки
`order:submit` - открытие окна заполнения контактных данных
`form:email` - заполнение электронной почты
`form:phone` - заполнение номера телефона
`buyer:changed` - изменения даннных покупателя
`contacts:submit` - открытие окна успешно оформленного заказа







